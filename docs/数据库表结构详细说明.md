# 数据库表结构详细说明

## 1. 用户表

用户表：存储用户的基本信息。表结构如表1所示。

表1  用户信息表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `user_id` | `BIGINT(20)` | 否 | 主键 | 用户ID |
| `username` | `VARCHAR(255)` | 否 | 否 | 用户名 |
| `password` | `VARCHAR(255)` | 否 | 否 | 密码 |
| `name` | `VARCHAR(255)` | 否 | 否 | 姓名 |
| `role` | `VARCHAR(255)` | 否 | 否 | 角色 |
| `department` | `VARCHAR(255)` | 是 | 否 | 院系 |
| `major` | `VARCHAR(255)` | 是 | 否 | 专业 |
| `contact` | `VARCHAR(255)` | 是 | 否 | 联系方式 |
| `max_students` | `INT` | 是 | 否 | 最大指导学生数 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 2. 师生互选表

师生互选表：存储师生互选的申请和绑定关系。表结构如表2所示。

表2  师生互选表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `selection_id` | `BIGINT(20)` | 否 | 主键 | 互选ID |
| `student_id` | `BIGINT(20)` | 否 | 外键 | 学生ID |
| `teacher_id` | `BIGINT(20)` | 否 | 外键 | 教师ID |
| `status` | `VARCHAR(255)` | 否 | 否 | 状态 |
| `apply_time` | `TIMESTAMP` | 是 | 否 | 申请时间 |
| `review_time` | `TIMESTAMP` | 是 | 否 | 审核时间 |
| `bind_time` | `TIMESTAMP` | 是 | 否 | 绑定时间 |
| `unbind_time` | `TIMESTAMP` | 是 | 否 | 解除时间 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 3. 选题表

选题表：存储学生提交的论文选题信息。表结构如表3所示。

表3  选题表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `topic_id` | `BIGINT(20)` | 否 | 主键 | 选题ID |
| `student_id` | `BIGINT(20)` | 否 | 外键 | 学生ID |
| `teacher_id` | `BIGINT(20)` | 否 | 外键 | 教师ID |
| `topic_name` | `VARCHAR(255)` | 否 | 否 | 选题名称 |
| `topic_description` | `TEXT` | 是 | 否 | 选题描述 |
| `review_status` | `VARCHAR(255)` | 否 | 否 | 审核状态 |
| `review_opinion` | `TEXT` | 是 | 否 | 审核意见 |
| `submit_time` | `TIMESTAMP` | 是 | 否 | 提交时间 |
| `review_time` | `TIMESTAMP` | 是 | 否 | 审核时间 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 4. 任务书表

任务书表：存储教师上传的任务书信息。表结构如表4所示。

表4  任务书表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `task_id` | `BIGINT(20)` | 否 | 主键 | 任务书ID |
| `topic_id` | `BIGINT(20)` | 否 | 外键 | 选题ID |
| `student_id` | `BIGINT(20)` | 否 | 外键 | 学生ID |
| `teacher_id` | `BIGINT(20)` | 否 | 外键 | 教师ID |
| `file_id` | `BIGINT(20)` | 是 | 外键 | 关联文件ID |
| `issue_time` | `TIMESTAMP` | 是 | 否 | 下达时间 |
| `deadline` | `TIMESTAMP` | 是 | 否 | 截止时间 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 5. 开题报告表

开题报告表：存储学生提交的开题报告信息。表结构如表5所示。

表5  开题报告表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `report_id` | `BIGINT(20)` | 否 | 主键 | 报告ID |
| `topic_id` | `BIGINT(20)` | 否 | 外键 | 选题ID |
| `student_id` | `BIGINT(20)` | 否 | 外键 | 学生ID |
| `teacher_id` | `BIGINT(20)` | 否 | 外键 | 教师ID |
| `file_id` | `BIGINT(20)` | 是 | 外键 | 关联文件ID |
| `submit_time` | `TIMESTAMP` | 是 | 否 | 提交时间 |
| `review_status` | `VARCHAR(255)` | 否 | 否 | 审核状态 |
| `review_opinion` | `TEXT` | 是 | 否 | 审核意见 |
| `review_time` | `TIMESTAMP` | 是 | 否 | 审核时间 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 6. 中期检查表

中期检查表：存储学生提交的中期检查信息。表结构如表6所示。

表6  中期检查表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `report_id` | `BIGINT(20)` | 否 | 主键 | 报告ID |
| `topic_id` | `BIGINT(20)` | 否 | 外键 | 选题ID |
| `student_id` | `BIGINT(20)` | 否 | 外键 | 学生ID |
| `teacher_id` | `BIGINT(20)` | 否 | 外键 | 教师ID |
| `file_id` | `BIGINT(20)` | 是 | 外键 | 关联文件ID |
| `submit_time` | `TIMESTAMP` | 是 | 否 | 提交时间 |
| `review_status` | `VARCHAR(255)` | 否 | 否 | 审核状态 |
| `review_opinion` | `TEXT` | 是 | 否 | 审核意见 |
| `review_time` | `TIMESTAMP` | 是 | 否 | 审核时间 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 7. 论文表

论文表：存储学生提交的最终论文信息。表结构如表7所示。

表7  论文表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `paper_id` | `BIGINT(20)` | 否 | 主键 | 论文ID |
| `topic_id` | `BIGINT(20)` | 否 | 外键 | 选题ID |
| `student_id` | `BIGINT(20)` | 否 | 外键 | 学生ID |
| `teacher_id` | `BIGINT(20)` | 否 | 外键 | 教师ID |
| `file_id` | `BIGINT(20)` | 是 | 外键 | 关联文件ID |
| `submit_time` | `TIMESTAMP` | 是 | 否 | 提交时间 |
| `review_status` | `VARCHAR(255)` | 否 | 否 | 审核状态 |
| `review_opinion` | `TEXT` | 是 | 否 | 审核意见 |
| `review_time` | `TIMESTAMP` | 是 | 否 | 审核时间 |
| `score` | `INT` | 是 | 否 | 成绩 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 8. 文件表

文件表：存储所有上传的文件信息。表结构如表8所示。

表8  文件表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `file_id` | `BIGINT(20)` | 否 | 主键 | 文件ID |
| `file_name` | `VARCHAR(255)` | 否 | 否 | 文件名称 |
| `file_path` | `VARCHAR(255)` | 否 | 否 | 文件路径 |
| `uploader_id` | `BIGINT(20)` | 否 | 外键 | 上传人ID |
| `receiver_id` | `BIGINT(20)` | 是 | 外键 | 接收人ID |
| `related_id` | `BIGINT(20)` | 是 | 否 | 关联ID |
| `file_type` | `VARCHAR(255)` | 是 | 否 | 文件类型 |
| `version` | `INT` | 是 | 否 | 版本号 |
| `sync_status` | `VARCHAR(255)` | 是 | 否 | 同步状态 |
| `upload_time` | `TIMESTAMP` | 否 | 否 | 上传时间 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 9. 进度表

进度表：存储学生论文各阶段的进度信息。表结构如表9所示。

表9  进度表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `BIGINT(20)` | 否 | 主键 | 进度ID |
| `student_id` | `BIGINT(20)` | 否 | 外键 | 学生ID |
| `stage` | `VARCHAR(255)` | 否 | 否 | 阶段名称 |
| `status` | `VARCHAR(255)` | 否 | 否 | 阶段状态 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 10. 通知表

通知表：存储系统发布的通知信息。表结构如表10所示。

表10  通知表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `notification_id` | `BIGINT(20)` | 否 | 主键 | 通知ID |
| `title` | `VARCHAR(255)` | 否 | 否 | 标题 |
| `content` | `TEXT` | 否 | 否 | 内容 |
| `publisher_id` | `BIGINT(20)` | 否 | 外键 | 发布人ID |
| `receive_role` | `VARCHAR(255)` | 否 | 否 | 接收角色 |
| `publish_time` | `TIMESTAMP` | 否 | 否 | 发布时间 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |

## 11. 系统设置表

系统设置表：存储系统的配置参数。表结构如表11所示。

表11  系统设置表
| 字段名称 | 数据类型 | 允许为空 | 主键或外键 | 字段说明 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | `BIGINT(20)` | 否 | 主键 | 设置ID |
| `setting_key` | `VARCHAR(255)` | 否 | 否 | 设置键 |
| `setting_value` | `VARCHAR(255)` | 否 | 否 | 设置值 |
| `description` | `TEXT` | 是 | 否 | 描述 |
| `created_at` | `TIMESTAMP` | 否 | 否 | 创建时间 |
| `updated_at` | `TIMESTAMP` | 否 | 否 | 更新时间 |